# 答辩记录整理

## 问答记录

问：
你觉得图中的三行内容是一个方法，还是分别表示不同结果？

答：
三行展示的是不同效果。第一行是本文方法的结果，第二行是基于规则处理后的结果，第三行是极限或对比结果。通过对比可以看到，本文方法相较于规则处理能够更好地保留目标。

问：
你这里说的“混淆”是什么意思？

答：
混淆指的是模型在航拍小目标场景下容易把相似类别识别错。例如语言提示中要求查找某类车辆时，模型可能会把外观相近的面包车、三轮车等目标混在一起。由于航拍视角下目标尺寸较小、细节不足，这类混淆更容易发生。

问：
“排序改善”是什么意思？

答：
排序改善是指对候选框的分数重新校准。原始模型在打分时，可能会把混淆框或重复框赋予较高分数，而一些真实但较小的目标反而得分偏低。后续校准模块会利用候选框的空间信息、类别信息、原始分数和排序信息，对候选框重新打分，从而提升真实目标的排序位置。

问：
你考虑的小目标到底有多小？

答：
目前没有给出非常精确的像素统计，这是后续需要补充的部分。总体上，航拍图像中的目标相对于整幅图像占比很小，例如三轮车等车辆目标在图中只占较少像素，因此识别和定位难度较高。后续应补充目标尺寸分布、平均像素占比等定量数据，并在展示图中放大目标区域，使问题更清晰。

问：
你刚才说目标分数仍然会有问题，具体是什么问题？

答：
问题主要在于原始得分不能完全代表候选框质量。部分错误候选框可能因为文本或视觉相似性获得较高分，而真实小目标由于细节不足得分偏低。因此需要通过反馈式校准器对候选框进行重新排序和重标定。

问：
你的论文题目是否合适？“强化学习”“闭环反馈”等表述是否准确？

答：
目前题目表述确实需要进一步调整。答辩中指出，“强化学习”更像是一种方法，而不是研究目标本身；“闭环”这一表述也容易让人误解系统中存在完整的强化学习闭环。本文实际关注的是无人机航拍场景下的开放词汇目标检测，重点是候选生成、候选排序校准和反馈优化。因此题目中应突出“无人机航拍目标检测”“开放词汇检测”“候选校准”或“反馈校准”等关键词，避免使用容易造成误解的表述。

问：
你所谓的“反馈”具体反馈了什么？

答：
反馈主要体现在候选框生成和排序校准两个环节。前一阶段产生的候选框、类别、原始分数、排序信息以及保留或误检情况会作为后续校准器的输入。校准器根据这些信息重新评估候选框的保留概率和最终分数，从而影响最终检测结果。这里的反馈更准确地说是候选结果到校准模块的特征反馈，而不是完整强化学习意义上的环境交互闭环。

问：
你的框架图中为什么有交叉箭头？这些箭头分别表示什么？

答：
框架图中的箭头需要重新梳理。目前交叉箭头容易造成误解，使人看不清候选生成、评估、反馈和校准之间的关系。更清晰的表达应是：开放词汇模型先生成候选框，然后将候选框的类别、分数、位置和排序信息输入校准模块，校准模块输出新的保留概率和最终排序结果。如果存在反馈，应明确说明反馈信息的来源、去向和作用。

问：
你用的基础模型是什么模型？

答：
基础模型使用的是现成的开放词汇检测模型，主要用于前端候选框生成。该部分没有对大模型进行微调，而是通过场景化提示词和类别提示词扩展候选生成能力。后续工作主要集中在候选框校准模块上，而不是训练或微调整个基础检测模型。

问：
后面的校准模型是什么模型？

答：
后面的校准模型是一个轻量级校准器，可以理解为基于候选框特征的简单分类或打分模型，例如多层感知机一类的结构。它的输入包括候选框位置、类别来源、原始分数、排序信息等，输出候选框的保留概率或校准后的最终分数。答辩中这部分没有讲清楚，后续需要明确模型结构、输入特征、输出目标、训练方式和损失函数。

问：
你说的 reward 是什么？为什么这里会提到 reward？

答：
目前对 reward 的表述不够准确。如果系统中没有真正的强化学习过程，就不应该直接使用 reward 这一术语。更准确的说法应是“校准分数”“候选保留概率”或“评价分数”。如果仍然使用 reward，需要明确它的定义、计算方式、监督信号来源，以及它如何参与模型训练或决策。

问：
你的方法到底做了哪些工作？

答：
本文主要做了三方面工作：第一，利用场景化提示词和类别提示词增强开放词汇模型在航拍场景中的候选召回能力；第二，保留候选框的负样本和中间信息，避免简单规则过滤造成有效目标丢失；第三，在固定候选框集合上训练轻量级校准器，对候选框分数和排序进行重新标定，从而提升最终检测质量。

问：
为什么不直接用规则删除错误候选框？

答：
直接规则删除虽然可以减少候选框数量，但容易误删真实目标，尤其是在小目标和相似类别较多的航拍场景中。实验结果表明，基于反馈特征的校准方法比固定规则删除更稳健，能够在保留更多有效目标的同时改善最终排序和检测结果。

问：
你的实验结果说明了什么？

答：
实验结果说明，方法收益并不主要来自候选框数量的增加，而是来自候选框质量和排序的改善。在相同候选框基础上，加入负信息融合和校准模块后，检测指标有所提升；相比之下，直接压缩候选框数量可能会删除有效目标，导致性能下降。

问：
类别提示词是不是越多越好？

答：
不是。补充贴近目标类别的提示词可以扩大匹配范围，提高部分小目标或易混淆类别的召回率。但提示词过多或类别过于相似时，也可能引入新的类别竞争和误检。因此提示词设计需要结合具体类别、场景和验证结果进行控制。

问：
PPT展示中存在哪些问题？

答：
主要问题包括：图中目标过小，老师难以看清；框架图箭头和模块关系不够清晰；“强化学习”“闭环”“reward”等术语解释不足；校准模型的结构和训练过程没有讲明白；实验图示中不同方法的含义没有提前交代清楚。后续应放大关键目标区域，重画模型框架图，并用更准确的术语说明方法流程。

问：
下一版需要重点修改什么？

答：
下一版需要重点修改题目、方法表述、框架图和模型说明。题目应突出实际研究对象和方法贡献；方法部分应清楚区分基础开放词汇检测模型、提示词扩展、候选框保留和后续校准器；框架图应减少歧义箭头；模型部分应明确校准器结构、输入输出、训练数据、损失函数和评价指标。同时，实验部分需要补充小目标尺度统计和更清晰的可视化结果。

## 后续修改建议

问：
论文题目可以如何调整？

答：
建议避免直接使用“强化学习闭环”这类容易引起误解的说法，可以改为围绕“无人机航拍”“开放词汇目标检测”“候选框校准”“反馈式排序优化”等关键词进行命名。例如：

1. 面向无人机航拍图像的开放词汇目标检测候选框校准方法研究
2. 基于候选反馈校准的无人机航拍开放词汇目标检测方法
3. 面向小目标航拍场景的开放词汇检测候选生成与排序校准方法

问：
答辩时应该如何解释自己的核心贡献？

答：
可以概括为：本文不是重新训练一个大型检测模型，而是在现有开放词汇检测模型基础上，针对无人机航拍小目标、类别混淆和候选排序不稳定的问题，设计了提示词增强的候选生成方法，并进一步利用候选框中间信息训练轻量级校准器，对候选框进行重打分和重排序，从而提升最终检测效果。
